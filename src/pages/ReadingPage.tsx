import { useNavigate } from "react-router-dom";
import styles from "./ReadigPage.module.css"; 

export default function ReadingPage() {

    return (
        <div className="mainContainer">
        </div>
    );
}
